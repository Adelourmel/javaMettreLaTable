In order to compile and execute the program, you have to add lib to classpath by doing this command :  

```bash
javac -cp "../lib/*" -d ../class ../src/JSONRead.java  
java -cp "../lib/*:../class/" JSONRead
```
